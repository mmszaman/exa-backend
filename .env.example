# Backend email configuration (copy this to backend/.env)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_smtp_app_password
ADMIN_TO=exateks@gmail.com
# Optional: set to 1 during local development to log emails instead of sending
DEV_FAKE_EMAILS=1
DEV_FAKE_LOG=sent_emails.log
# Allow origins (comma separated) for CORS
FRONTEND_ORIGINS=http://localhost:3000
